apiVersion: v1
kind: Service
metadata:
  name: bet-settlement-service
  namespace: default
  labels:
    app: bet-settlement-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: bet-settlement-service
