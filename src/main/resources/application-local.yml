spring:
  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

rocketmq:
  enabled: false
  name-server: localhost:9876

logging:
  level:
    com.sporty.betting: DEBUG
